# A sample Gemfile
source "https://rubygems.org"

group :test do
  if RUBY_PLATFORM =~ /(win32|w32)/
    gem 'win32console'
  end
  gem 'minitest'
  gem 'minitest-reporters', '>= 0.5.0'
end

gem 'slop'
gem 'chronic_duration'